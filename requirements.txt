# ============================================================
# HR Copilot — Multi-Agent Employee Self-Service System
# WS01 · MLDS 2026 · Maneesh Kumar & Ravikiran Ravada
# ============================================================
# Python 3.10+ required
# Install: pip install -r requirements.txt

# ── Vector & Keyword Search ──────────────────────────────────
faiss-cpu>=1.7.4
rank-bm25>=0.2.2

# ── Embeddings, Reranking, NLI ───────────────────────────────
sentence-transformers>=2.7.0
transformers>=4.40.0
torch>=2.0.0

# ── Data Handling ─────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0

# ── Document Loading ─────────────────────────────────────────
pypdf>=4.0.0
python-docx>=1.1.0

# ── Text Splitting ───────────────────────────────────────────
langchain-text-splitters>=0.2.0
tiktoken>=0.7.0

# ── LLM (local via Ollama) ────────────────────────────────────
# ollama is optional — pipeline uses template fallback if not installed
# Uncomment below if you have Ollama installed and want richer LLM answers:
# ollama>=0.1.9
langchain>=0.2.0
langchain-community>=0.2.0

# ── Evaluation ───────────────────────────────────────────────
# RAGAS-style metrics are computed locally (NLI + cosine) without API keys
# datasets is a dependency of some langchain components
datasets>=2.18.0

# ── Utilities ────────────────────────────────────────────────
tqdm>=4.66.0
colorama>=0.4.6
python-dotenv>=1.0.0
tabulate>=0.9.0

# ── Web UI ────────────────────────────────────────────────────
streamlit>=1.35.0
